<h1>
	<p align = "center">
    <%= gravatar_for @user%>
		</br><%= @user.username %>
	</p>
</h1>

<% if @user.posts.any? %>
	<h3>Posts (<%= @user.posts.count %>)</h3>
	<ol>
    <%= render @posts %>
  </ol>
	<%= will_paginate @posts %>
<%end%>
