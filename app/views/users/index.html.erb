<h1>All users</h1>

<%= will_paginate %>
<u>
  <% @users.each do |user| %>
		<li>
			<%= gravatar_for user, size: 50 %>
			<%= link_to user.username, user_path(user) %>
		</li>
  <% end %>
</ul>
<%= will_paginate %>